apiVersion: v1
kind: Service
metadata:
  name: hybrid-ai-app-service
  namespace: default
spec:
  selector:
    app: hybrid-ai-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP
